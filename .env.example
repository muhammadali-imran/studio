# API base URL
VITE_API_URL=http://localhost:8000/api

# When true, pages fall back to bundled mock data if the API call fails
# or returns empty during local development. Never set this in production.
VITE_USE_MOCKS=true
